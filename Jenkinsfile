pipeline {
   agent any

    stages {
        stage('Build') {
            steps {
                   
                   echo "hi the branch is ${BRANCH_NAME}"
                   sh 'printenv'

            }
   
        }
    }
}
