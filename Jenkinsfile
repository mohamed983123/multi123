pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                   echo '${BRANCH_NAME}'
                   echo "hi the branch is ${BRANCH_NAME}"

            }
   
        }
    }
}
